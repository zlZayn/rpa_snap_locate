# atoms/ — 规则层

继承根规则，见 [../../AGENTS.md](../../AGENTS.md)。

atoms/ 特有约束：
- 一个原子只完成一个可独立验证的动作，不复制其他原子的内部实现
- 通过参数接收路径和业务数据，不写死具体软件或工作流
- 成功时正常结束，失败时 `throw` 或返回非零退出码
- 文件名使用小写短横线，PowerShell 参数使用 PascalCase
- 不写"有什么文件/怎么改"，那是 [README.md](README.md) 的职责
